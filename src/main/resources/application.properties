spring.application.name=BlogBackend

# Veritaban? Ba?lant? Ayarlar? (Docker Uyumlu)
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:blog_db}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:3519}

# Hibernate Ayarlar?
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# JWT Ayarlar?
# Secret key en az 256 bit (32 karakter) uzunlu?unda karma??k bir metin olmal?d?r.
jwt.secret=RidvanKarsliBlogBackendSuperSecretKeyForJwtAuthentication2026!
# Token geçerlilik süresi (Örn: 1 Gün = 86400000 milisaniye)
jwt.expiration=86400000
# Cookie ad?
jwt.cookie-name=blog_jwt

# Dosya Yükleme Ayarlar?
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
media.upload-dir=uploads